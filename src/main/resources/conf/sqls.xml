<?xml version="1.0" encoding="UTF-8"?>
<sqls xmlns="http://www.leapframework.org/schema/sqls">
    <command key="user.all">
        select * from User
        where name = #userName#
        {? and age > #age# }
    </command>
    <command key="user.some">
        select * from User where 1 = 1
        {? and id = :id }
        {? and name = :name }
        {? and age = :age }
        {? order by :field}
    </command>
</sqls>